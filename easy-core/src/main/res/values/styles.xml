<resources>

    <!-- B应用主题样式 -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- 窗口背景颜色 -->
        <item name="android:windowBackground">@color/color_white</item>
        <!-- 应用的主要色调，ActionBar Toolbar 默认使用该颜色 -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <!-- 应用的主要暗色调，状态栏默认使用该颜色 -->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <!-- 应用的强调色，CheckBox RadioButton Switch 等一般控件的选中效果默认采用该颜色 -->
        <item name="colorAccent">@color/colorAccent</item>
        <!-- ActionMode 覆盖 Actionbar 不被顶下来 -->
        <item name="windowActionModeOverlay">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- Activity 专场动画  -->
        <item name="android:windowAnimationStyle">@style/AnimationActivity</item>
    </style>

    <!-- 闪屏页主题样式 -->
    <style name="SplashTheme" parent="AppTheme">
        <item name="android:windowBackground">@drawable/app_bg</item>
    </style>

    <style name="AnimationActivity" parent="@android:style/Animation">
        <!-- A 打开 B，B 的出现动画 -->
        <item name="android:activityOpenEnterAnimation">@anim/slide_enter</item>
        <!-- A 打开 B，A 的消失动画 -->
        <item name="android:activityOpenExitAnimation">@anim/slide_exit</item>
        <!-- B 关闭到 A，A 的出现动画 *时长要小于 B 的消失动画，否则会闪烁* -->
        <item name="android:activityCloseEnterAnimation">@anim/slide_pop_enter</item>
        <!-- B 关闭到 A，B 的消失动画 *时长要小于 B 的消失动画，否则会闪烁* -->
        <item name="android:activityCloseExitAnimation">@anim/slide_pop_exit</item>
    </style>
</resources>
